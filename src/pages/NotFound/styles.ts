import styled from 'styled-components';

export const PageNotFound = styled.main`
  background-color: ${(props) => props.theme.colors.primary};
`;

export const Container = styled.div`
  padding: 1.875rem 0;
`;

export const ImageAlert = styled.img`
  max-height: 500px;
  min-height: 350px;
  margin: 0 auto;
`;
